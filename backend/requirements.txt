# fastapi bumped to 0.136.1 (unblocks starlette >=1.0).
# starlette bumped to 1.3.1: clears CVE-2025-54121, CVE-2025-62727, PYSEC-2026-161,
#   CVE-2026-48817, CVE-2026-48818, CVE-2026-54282, CVE-2026-54283.
fastapi==0.136.1
starlette==1.3.1  # transitive (fastapi); pins all known CVE fixes as of 2026-06-25
uvicorn[standard]==0.46.0
python-dotenv==1.2.2
pydantic==2.11.7
pydantic-settings==2.7.1
supabase==2.31.0
httpx==0.27.0
openpyxl==3.1.5
python-multipart==0.0.32  # bumped 0.0.28->0.0.32; clears CVE-2026-53537/38/39/40

# Wasden Watch RAG pipeline
pymupdf==1.25.3
chromadb==0.6.3
anthropic==0.43.0
google-generativeai==0.8.6
tiktoken==0.9.0
sentence-transformers==3.4.1
colorama==0.4.6

# Week 3 screening + Week 8 risk/execution
finnhub-python==2.4.29
yfinance==0.2.36
alpaca-py==0.43.4

# Week 5 quant models
scikit-learn==1.6.1
xgboost==2.1.4
statsmodels==0.14.6
newsapi-python==0.2.7

# Week 7 decision pipeline
# langgraph held at 0.2.0 deliberately. Bumping to 1.0.10 (clears
# CVE-2026-28277) requires a deliberate StateGraph + node-signature
# port tracked as task #51 in TODO.md. The langchain-core /
# langgraph-checkpoint / langsmith CVEs ride along with that port and
# are allowlisted together (revisit 2026-05-25). DO NOT bump this
# cluster independently — the four packages must move as a set.
langgraph==1.2.6

# MLflow experiment tracking and model registry
# Bumped 2.10.0 -> 2.22.4 (latest 2.x). Avoided 3.x major jump per triage notes.
# Transitive pins force fixed gunicorn / protobuf / pyarrow.
mlflow==2.22.4
gunicorn==22.0.0  # transitive (mlflow); pins CVE-2024-6827, CVE-2024-1135 fix
protobuf==5.29.6  # transitive (mlflow); pins CVE-2026-0994 fix
pyarrow==17.0.0   # transitive (mlflow); pins PYSEC-2024-161 fix

# Rate limiting
slowapi==0.1.10

# Self-hosted Postgres client — replaces Supabase data layer (SSS-T008)
psycopg[binary]==3.3.4
psycopg-pool==3.3.1
